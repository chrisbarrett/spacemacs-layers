# key: cl
# name: scalatest class (WordSpec)
# condition: (yas/bol?)
# --
import org.scalatest.{ FunSpec, Matchers }

class ${1:Name}Test extends WordSpec with Matchers {
  "${1:$(if (and yas/text (s-matches? "^[aeiouAEIOU]" yas/text)) "An" "A")} $1" when {
    "${2:behaviour}" should {
       "${3:effect}" in {
          true should be (false)
       }
    }
  }
}
