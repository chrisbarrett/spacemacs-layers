# key: cl
# name: scalatest class (WordSpec)
# condition: (yas/bol?)
# --
class ${1:Name}Test extends WordSpec with ShouldMatchers {
  "${1:$(if (and yas/text (s-matches? "^[aeiouAEIOU]" yas/text)) "An" "A")} $1" when {
    "${2:behaviour}" should {
       "${3:effect}" in {
          new Fixture {
            $0
            true should be (false)
          }
       }
    }
  }

  trait Fixture {}
}
