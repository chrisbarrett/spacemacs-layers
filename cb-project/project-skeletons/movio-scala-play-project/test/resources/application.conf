# This is the configuration file used when running tests.
# Settings in this file will override those in the reference.conf.
# ~~~~~

# Disable noisy akka logging during tests
# ~~~~~
akka {
  log-dead-letters = 0
  log-dead-letters-on-shutdown = false
  log-level = DEBUG
}
