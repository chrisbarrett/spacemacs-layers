# -*- mode: snippet -*-
# name: else-if
# key: ei
# condition: (or (yas/bol?) (s-matches? (rx (* space) "}" (* space) "ei" (* space) eos) (buffer-substring (line-beginning-position) (point))))
# --
else if $1 {
  $0
}